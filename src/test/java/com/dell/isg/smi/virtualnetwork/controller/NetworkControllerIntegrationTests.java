/**
 * Copyright © 2017 DELL Inc. or its subsidiaries.  All Rights Reserved.
 */
package com.dell.isg.smi.virtualnetwork.controller;

//@RunWith(SpringJUnit4ClassRunner.class)
//@RunWith(SpringRunner.class)
//@WebIntegrationTest({"server.port=0", "management.port=0"})
//@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT);
public class NetworkControllerIntegrationTests {
    // extends AbstractTest {
    /*
     * @Value("${local.server.port}") private int port;
     * 
     * private RestTemplate restTemplate = new TestRestTemplate();
     * 
     * //MockRestServiceServer server = MockRestServiceServer.bindTo(restTemplate).build();
     * 
     * @Mock private NetworkConfigurationManager networkConfigurationManager = Mockito.mock(NetworkConfigurationManager.class);
     * 
     * //@Before public void setup(){
     * 
     * List<Network> networkList = new ArrayList<Network>(); networkList.add(new Network()); networkList.add(new Network()); PagedResult pagedResult =
     * PaginationUtils.paginate(networkList, 2, 0, 10); when( this.networkConfigurationManager.getAllNetworks(0, 10) ).thenReturn( pagedResult ); }
     * 
     * //@Test public void testRequest() throws Exception {
     * 
     * NetworkController networkController = new NetworkControllerImpl();
     * 
     * HttpHeaders headers = restTemplate.getForEntity("http://localhost:" + port + "/networks", String.class).getHeaders(); String response =
     * restTemplate.getForObject("http://localhost:" + port + "/networks", String.class); assertNotNull(response); System.out.print(response);
     * //assertThat(headers.getLocation().toString(), containsString("myotherhost")); }
     */
}
